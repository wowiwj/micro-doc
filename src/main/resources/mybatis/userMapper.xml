<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wangju.microdoc.model.domain.UserRepository">

    <sql id="base_column_list">
        id,
        username,
        password,
        sex,
        bio
    </sql>

    <select id="get" parameterType="long" resultType="user">
        select
        <include refid="base_column_list"/>
        from "user"
        where id = #{id}
    </select>

    <select id="getByName" parameterType="string" resultType="user">
        select
        <include refid="base_column_list"/>
        from "user"
        where username = #{username}
    </select>

    <select id="all" resultType="wangju.microdoc.model.domain.User">
        select
        <include refid="base_column_list"/>
        from "user"
    </select>

    <insert id="create" parameterType="wangju.microdoc.model.domain.User">
        insert into "user" (username, password, bio, last_active_time)
        VALUES (#{username}, #{password}, #{bio}, #{lastActiveTime})
    </insert>

</mapper>